<h1><%= @heading %></h1>

<p><%= _('The translation strings you can manage through this ' +
       'interface are automatically generated by the catalog and field ' +
       'values present in your database, and thus, manually adding ' +
       'strings is not supported.') %></p>

<p><%= _('In order to ensure consistency, when you delete a string in ' +
	  'any of the supported languages, it will be removed in all ' +
	  'of them. However, every time a given string is required by ' +
	  'the system, and every time you visit the %s page, all ' +
          'of the needed strings will be created again.') %
       link_to(_('Translation statistics by language'),
                 :action => 'stat_by_lang') %></p>

<%= pagination_header(@strings) %>
<%= will_paginate @strings %>
<% if ! @strings.empty? %>
  <% table do %>
    <% table_head_row do %>
      <th><%= _('Base') %></th>
      <th><%= _('Translated') %></th>
      <th></th>
    <% end %>
    <% @strings.each do |str| %>
      <% form_for :translation, str,
                  :url => {:action => 'update', :id => str} do |f| %>
        <% table_row do %>
          <td><%= str.base %></td>
          <td><%= f.text_field :translated %></td>
	  <td><%= f.submit _('Update') %>
	    <%= link_to _('Delete'), {:action => 'delete', :id => str},
	      {:confirm => _('Are you sure you want to remove this record?'),
	     :method => :post} %>
	  </td>
        <% end %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
